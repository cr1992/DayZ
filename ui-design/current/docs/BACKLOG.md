# DayZ 待办 / Backlog

> 记录**尚未动手或未定档**的需求与交互缺口。定档后从这里移除，并按规矩写入 `CHANGELOG.md`。
> 与 `CHANGELOG.md`（已定档的事实）互补：这里是「要做的」，那里是「做完的」。

---

## 功能需求

### [背景/纸色] 每个日记本独立纸色 〔待定〕
现纸色（`data-bg`）为**全局一套**。Day One 式的进阶：每个日记本（journal）可各自选纸色，进入某本时背景随之切换。
- **待确认**：是否与 journal 既有的 `color` 字段联动（本色 → 自动「主题微染」种子）？还是独立第二个字段（`paper` + `paperSeed`）。
- **Flutter 落地**：journal 表加 `paper` / `paper_seed`；进入某本时把背景族 override 到该本纸色（局部 Theme）。
- 切日记本时背景过渡，避免突兀（淡入或随列表刷新一起）。

### [往年今日] 生成回忆卡片图 〔✅ 原型已完成，本项可移除〕
已定档：往年今日 ⋯ → `memory.html`，画幅（竖版 9:16 / 方形 1:1）+ 风格（纸感 / 大图压字）可切，预览 + 保存/分享 toast。
- **落地待做**（Flutter）：卡片 widget 走 `RepaintBoundary.toImage()` → PNG → `share_plus` / 存相册；真导出。
- **可选进阶**（待定）：当日合集卡、更多画幅/配色、每条卡片右下分享小图标入口。

---

## 交互缺口

> 本轮已集中填完（⋯ 菜单 / 收藏 toggle / 设置选择器 / 抽屉视图 / 新建日记本），详见 CHANGELOG。仅余下面一项，原型阶段刻意保留静态。

1. **编辑器录入态** 〔原型阶段可接受〕 —— 富文本为静态展示，无真实录入；落地走 **AppFlowy Editor**（方案 A）。原型不再模拟实时键入。

### [编辑器] 原生编辑页对齐设计 〔待原生落地〕
原型二级菜单（标题/颜色/链接内联面板）+ 全屏图片选择器 + 大图查看器已定档，走查发现原生 `lib/ui/editor/*` 有 5 处漂移待修，完整差异 + 改法 + 色板 hex + 验收清单见 **`docs/handoff/editor.md`**。
- 🔴 颜色/高亮传 DayZ 暖调色板（替掉 AppFlowy 默认 Material 原色）。
- 🟠🟡 meta chip / 日期 / 关闭回到内联 SVG（补 4 条 `DayzIcons` path）。
- 🟡 标题菜单加「正文」项；图片插入改为全屏微信式选择器（首格相机 + 多选编号），内容图接大图查看器。
