# 屏 / 功能 spec · `verification.md` 验收分层骨架（可复制）

> 作者：@Ray ｜ 关联 spec：[`e2e-harness`](./)（T4 / R1 / R5 / R6）
>
> 这是新屏 / 功能 spec 写 `verification.md` 时**可直接复制**的两栏骨架，落实 R1：把验收项显式切成
> **「自动化可覆盖」**（widget test 或 Patrol E2E 能断言）与 **「必须人工」**（设计目检 + 安全·不可逆链路终验），不得混写。
>
> 判据：纯 in-Flutter 行为 → widget test 即可，**不**强制 E2E；**有原生跨界 / 不可逆副作用**的链路 → 标「需 E2E」并
> `dependsOn e2e-harness`、用例放 `patrol_test/`、经 `scripts/patrol_test.sh` 跑（R7 校验 `Total:` 非零）。
> **安全 / 不可逆链路（加密 / 备份 / 还原）即便 E2E 全绿仍保留人工终验**（patrol 有静默假阳性 + iOS 模拟器 CI flaky 先例，R6）。

---

```markdown
---
作者：@Ray
创建日期：YYYY-MM-DD
最后更新：YYYY-MM-DD
文档状态：进行中
---

# 验证：<feature>

> 本文件落「需多任务产物同时成立 / 专项跨任务」的集成与端到端验证；单任务可独立验证的在 `tasks.md` 各任务内，不在此重复。
> 验收按 R1 分层：每行「方式」列标 **自动** 或 **人工**；人工项注明核查人。

## 功能验证（端到端，跨任务）

| 场景 | 操作 | 预期结果 | 关联需求 | 方式 |
|------|------|----------|----------|------|
| <纯 in-Flutter 行为> | <pump / 点击> | <断言可观测结果> | R? | 自动 |
| <原生跨界：相册/权限/文件IO/Keychain/前后台> | <进屏触发该链路> | <Patrol 处理原生弹窗 + 落库断言> | R? | 自动（E2E，依赖 e2e-harness） |
| <设计目检：像素/间距/配色/动画手感> | <真机走查> | <对齐设计稿源屏> | R? | 人工（@Ray） |
| <安全·不可逆：加密/备份/还原终验> | <真机演示/回归> | <数据可还原、密文正确> | R? | 人工（@Ray，E2E 绿也不撤此闸） |

## 专项检查
> 对应 requirement 的 NF 编号。验收命令验**行为**、不可被字面满足糊弄（禁 grep 被改文件自身内容）。

### <NF 主题>（NF?）
- [ ] <可观测断言> — 自动：`flutter test test/ui/<feature>/<x>_test.dart`（断言对应 R/NF 的具体结果，非空壳）
- [ ] <原生链路冒烟> — 自动：`bash scripts/patrol_test.sh -d <device> --target patrol_test/<x>_test.dart`（R7 守卫 Total 非零）
- [ ] <设计 / 不可逆终验> — 人工（@Ray）：<核查口径>
```

---

## 套用要点

- **两栏不可混写**：一条验收项要么落「自动」要么落「人工」，跨界链路的「功能回归」截面可自动化（标自动·E2E），「设计目检 / 不可逆终验」截面留人工。
- **DoD 差异化（R5）**：有原生跨界 / 不可逆副作用 → 加「主链路 E2E 冒烟通过」；纯 in-Flutter → 维持 widget test 基线，不强制 E2E。
- **人闸不可因绿撤（R6）**：加密 / 备份 / 还原即便 E2E 全绿，仍保留人工终验。
- **抗规避**：自动项必须真断言需求的可观测结果（`assert(true)` / grep 被改文件 = 假壳），口径见 [`spec-kit/spec-guide.md`](../../../spec-kit/spec-guide.md) P3。
